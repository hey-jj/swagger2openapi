//! The conversion orchestration and the process helpers.
//!
//! This module will hold `convert_obj`, the `main` conversion core, and the
//! `process_*` helpers for parameters, responses, headers, paths, and
//! security, plus `fix_info`, `fix_paths`, `extract_server_parameters`, and
//! `detect_object_references`.
