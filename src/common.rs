//! String helpers, the 32-bit hash, and the constant tables.
//!
//! This module will hold `sanitise`, `sanitise_all`, the UTF-16 `hash`,
//! `to_camel_case`, `unique_only`, and the constant tables
//! (`parameterTypeProperties`, `arrayProperties`, `httpMethods`).
